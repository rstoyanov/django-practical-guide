pip install Django

# call the admin functions
django-admin

#create a project (Navigate to the app folder first)
django-admin startproject mypage

# run the web server (Navigate to the app folder first)
python3 manage.py runserver

# Create a new app/module/feature in the same django page
python3 manage.py startapp challanges

